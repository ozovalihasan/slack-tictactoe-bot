require_relative '../lib/api'

describe API do
  describe '#send_response' do
    it 'prints the game board' do
      # This method is used to connect workspace and app.
      # But details of this code is not known.
      # Bot couldn't be created without this code, so it is not removed
    end
  end
end
